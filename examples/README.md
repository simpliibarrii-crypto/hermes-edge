# Examples

## hello_hermes.py

Minimal example: load a `.litertlm` model and run a simple query with tool support.

```bash
python examples/hello_hermes.py
```

Requires a model file at `dist/hermes-mobile-270m-int4.litertlm`.
